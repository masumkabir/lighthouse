version: 0.2

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - echo Lighthouse app is Building
  post_build:
    commands:
      - echo Build completed on `date`

artifacts:
 type: zip
 files:
  - *